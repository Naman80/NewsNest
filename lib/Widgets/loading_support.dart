import 'package:flutter/material.dart';

class LoadingSupport extends StatelessWidget {
  const LoadingSupport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
