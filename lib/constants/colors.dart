import 'package:flutter/material.dart';

class ColorPicker {
  static const primary = Color(0xFFE0DCDD);
  static const secondary = Color(0xFF9B8A8E);
}

class TextColorPicker {
  static const primary = ColorPicker.primary;
  static const secondary = ColorPicker.secondary;
  static const link = Color(0xFF96ADE7);
}

class IconColorPicker {
  static const primary = ColorPicker.primary;
  static const secondary = Color(0xFF1a1a1a);
}

class BgColorPicker {
  static const primary = Color(0xFF0d0d0d);
  static const secondary = Color(0xFF1a1a1a);
  static const selectedNav = ColorPicker.primary;
}
